# 脚本目录说明

`scripts/` 用于保存离线构建、数据整理和一次性但可复现的工具。正式用户入口优先放在
`app/`，可复用的核心逻辑优先放在 `src/`；脚本不应成为绕过正式入口的第二套实现。

脚本应：

- 通过参数接收输入和输出路径，不硬编码个人电脑目录；
- 使用仓库相对路径或显式传入的路径；
- 按 `outputs/README.md` 的约定保存普通实验结果；
- 在说明或帮助信息中写清输入、输出和复现命令；
- 不提交临时数据库、缓存、API Key 或无法追溯的数据。

本目录当前只有协作说明，不代表第二周脚本已经完成。
