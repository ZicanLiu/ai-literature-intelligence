# W2 执行记录

**日期:** 2026-07-30
**最终状态:** 45 测试通过, 120 条数据验证通过

---

## 环境

- Python 3.12.4 / Windows 10 (PowerShell 5.1)
- 依赖: requests, pandas, matplotlib, python-dotenv

---

## 最终验证

```powershell
python -m unittest discover -s tests/automated -p "test_*.py" -v
```
**45 tests OK** (38 dedup + 7 output_runs)

### 结果生成

```powershell
python -m app.review_duplicates --generate
```

```
加载论文: 120 条
确定重复: 11 条 (same_openalex_id: 11, same_doi: 0, same_title_no_id: 0)
疑似重复: 2 对
数量验证通过: 120 = 109 + 11
来源分布: run001_ml_stellar_spectra: 60, run002_dl_stellar_spectroscopy: 60
```

---

## 修复记录 (v2)

1. **run_id 修正**: 重新从两份原始查询 CSV 生成 combined_w2_raw.csv，run001 和 run002 各 60 条
2. **空相似度返回 0.0**: jaccard/sequence/author_overlap 在双方为空时返回 0.0 而非 1.0
3. **same_title_no_id 限制**: 仅当双方 openalex_id 和 doi 都为空时自动合并
4. **空标题跳过**: 疑似匹配时任意一侧标准化标题为空则跳过
5. **测试强化**: 检查具体 ID/pair/run_id 值，新增 7 个测试（27 → 34）
6. **结果生成入口**: `app/review_duplicates.py --generate` 一键重新生成所有 CSV
7. **CLI 回归修复**: 补充 `--stats`，并覆盖 `--generate`、`--list`、`--stats`
   与未知参数四个非交互场景

---

## 文件清单

```
src/deduplication.py
app/review_duplicates.py
tests/automated/test_deduplication.py
tests/fixtures/dedup/test_papers.json
data/samples/w2/dedup/
data/review/suspected_duplicates_w2.csv
data/analysis/w2_dedup/
docs/project/DEDUPLICATION_DESIGN.md
docs/reports/week2/DEDUPLICATION_W2.md
w2.md
```
