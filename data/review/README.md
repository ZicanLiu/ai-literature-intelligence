# 人工复核队列

本目录用于保存需要人工复核的结构化队列，例如疑似重复对和其他待确认记录。

`review_status` 统一使用以下允许值：

- `待复核`：尚未完成人工判断；
- `已确认`：已经按任务规则完成复核；
- `有争议`：证据不足或成员判断不一致。

未复核记录不能表述为已确认。疑似重复只进入复核队列，不得因为相似度或其他自动规则
直接删除；每条记录还应保留能追溯到原始论文和实验的标识。
